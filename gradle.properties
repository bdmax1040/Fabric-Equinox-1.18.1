# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G

# Fabric Properties
	# check these on https://fabricmc.net/develop
	minecraft_version=1.18.1
	yarn_mappings=1.18.1+build.18
	loader_version=0.12.12

# Mod Properties
	mod_version = 1.0.0-alpha
	# This mod should stay in alpha before play testing.
	# Version numbers can be increased for each new complete build, and can be added into the releases on the Git Repository
	maven_group = net.bdmax
	archives_base_name = equinox

# Dependencies
	fabric_version=0.46.0+1.18
